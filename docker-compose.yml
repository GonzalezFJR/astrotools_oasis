services:
  web:
    build: .
    ports:
      - "${APP_PORT:-8000}:8000"
    env_file:
      - .env
    volumes:
      - .:/app
      - astrotools_projects:/tmp/astroeditor_projects
    restart: unless-stopped

volumes:
  astrotools_projects:
